# Memos Setup
HOST=tower
PORT=5232
MEMOS_HOST=${HOST}
MEMOS_PORT=${PORT}
MEMOS_URL=http://${MEMOS_HOST}:${MEMOS_PORT}
MEMOS_TOKEN=ehidden_token_but_does_exist_here

# Server Configuration
HOST=localhost
PORT=8002
DEBUG=true

# API Configuration
API_PREFIX=/api/v1
CORS_ORIGINS=["http://${MEMOS_HOST}:3000", "http://${MEMOS_HOST}:8080"]

# Database (if needed later)
# DATABASE_URL=sqlite:///./memos.db
#
MEMOTIC_CLI_CONTAINER=memotic-cli-sandbox
MEMOTIC_CLI_WORKDIR=/workspace
MEMOTIC_CLI_SHELL=/bin/bash
MEMOTIC_CLI_TIMEOUT=30


MEMOTIC_API_BASE=${MEMOS_URL}${API_PREFIX}
MEMOTIC_API_TOKEN=${MEMOS_TOKEN}
